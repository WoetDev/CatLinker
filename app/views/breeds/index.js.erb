function replaceBreeds (innerHTML) {
  var breeds = $('#breeds');
  $('.preloader-wrapper').removeClass('active');
  $(breeds).html('');
  $(innerHTML).appendTo(breeds).hide().fadeIn(400);
}

replaceBreeds("<%= j(render 'page_items')%>");

