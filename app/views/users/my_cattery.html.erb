<div class="container">
  <div class="form">
    <div class="row">
      <h2 class="form-title">My Cattery</h2>
    </div>

    <%= form_for(@user, url: my_cattery_cattery_path(@user), html: { method: :post }) do |f| %>
      <div class="row">
        <br>
      </div>

      <div class="row">
        <h4 class="form-section-title">General</h4>
      </div>

      <div class="row">
        <div class="col s12 l8">
          <div class="input-field">
            <%= f.label :cattery_name do %>
              Cattery name *
            <% end %>
            <%= f.text_field :cattery_name, autofocus: true %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :cattery_name %>
        </div>

        <div class="col s12 l4">
          <div class="input-field">
            <%= f.label :certification_number %>
            <%= f.text_field :certification_number %>
          </div>
        </div>
      </div>

      <div class="row my-cattery-pages">
        <% if f.object.cattery_name.present? and f.object.postal_code.present? and f.object.city.present? and f.object.country_id.present? %>
          <div class="col">
            <%= link_to overview_cattery_path(@user), class:"waves-effect waves-light btn-large btn-link" do %>
              Edit cats
              <i class="material-icons right">create</i>
            <% end %>
          </div>
          <% if @user.cats.any? %>
            <div class="col">
              <%= link_to cattery_path(@user), class:"waves-effect waves-light btn-large btn-link" do %>
                View page
                <i class="material-icons right">remove_red_eye</i>
              <% end %>
            </div>
          <% end %>
        <% else %>
          <div class="col">
            <button class="btn-large disabled ">
              Edit cats
              <i class="material-icons right">create</i>
            </button>
          </div>
          <div class="col">
            <div class="blocking-helper-text"><%= @required_info_message %></div>
          </div>
        <% end %>
      </div>

      <div class="row">
        <h4 class="form-section-title">Profile picture</h4>
      </div>

      <div class="row">
        <div class="col s12">
          <div class="file-field input-field">
            <div class="btn">
              <i class="material-icons-outlined">add_photo_alternate</i>
              <span>Profile picture</span>
              <%= f.file_field :profile_picture %>
            </div>
            <div class="file-path-wrapper">
              <input class="file-path validate" type="text">
            </div>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col s12">
          <div class="card-picture-preview">
            <% if @user.profile_picture.blank? %>
              <span>Preview *</span>
            <% else %>
              <%= image_tag(@user.profile_picture) if @user.profile_picture.attached? %> 
            <% end %>
            <%= render 'shared/error_messages', object: @user, field: :profile_picture %>
          </div>
        </div>
      </div>

      <div class="row">
        <h4 class="form-section-title">Address</h4>
      </div>

      <div class="row">
        <div class="col s9">
          <div class="input-field">
            <%= f.label :street %>
            <%= f.text_field :street %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :street %>
        </div>

        <div class="col s3">
          <div class="input-field">
            <%= f.label :number %>
            <%= f.text_field :number %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :number %>
        </div>
      </div>

      <div class="row">
        <div class="col s5">
          <div class="input-field">
            <%= f.label :postal_code do %>
              Postal code *
            <% end %>
            <%= f.text_field :postal_code %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :postal_code %>
        </div>
        
        <div class="col s7">
          <div class="input-field">
            <%= f.label :city do %>
              City *
            <% end %>
            <%= f.text_field :city %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :city %>
        </div>
      </div>

      <div class="row">
        <div class="col s12 m5">
          <div class="input-field">
            <%= f.select(:country_id, options_for_select(@all_countries, selected: f.object ? f.object.country_id : ""), {selected: "", disabled: "", prompt: 'Country *'}) %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :country_id %>
        </div>
      </div>

      <div class="row">
        <h4 class="form-section-title">Contact &amp; social media</h4>
      </div>

      <div class="row">
        <div class="col s12 m6">
          <%= link_to @change_email_path, class:"tooltipped", :data => { :'position' => "bottom", :'tooltip' => @change_email_tooltip }  do %>
            <div class="input-field">
              <%= f.label :email %>
              <%= f.text_field :email, class:"disabled-input", disabled: true %>
            </div>
          <% end %>
        </div>
        
        <div class="col s12 m6">
          <div class="input-field">
            <%= f.label :phone_number %>
            <%= f.text_field :phone_number %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :phone_number %>
        </div>
      </div>

      <div class="row">
        <div class="col s12 l4">
          <div class="input-field">
            <i class="fab fa-facebook-square prefix"></i>
            <%= f.label :facebook_link %>
            <%= f.text_field :facebook_link %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :facebook_link %>
        </div>

        <div class="col s12 l4">
          <div class="input-field">
            <i class="fab fa-instagram prefix"></i>
            <%= f.label :instagram_link %>
            <%= f.text_field :instagram_link %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :instagram_link %>
        </div>
        
        <div class="col s12 l4">
          <div class="input-field">
            <i class="fab fa-twitter prefix"></i>
            <%= f.label :twitter_link %>
            <%= f.text_field :twitter_link %>
          </div>
          <%= render 'shared/error_messages', object: @user, field: :twitter_link %>
        </div>
      </div>

      <div class="row">
        <div class="actions">
          <%= f.button class:"btn waves-effect waves-light", type:"submit" do %>
          Update
          <i class="material-icons right">send</i>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>